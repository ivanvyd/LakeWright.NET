# 1.1.0 release evidence

The pre-publication regression ran on 2026-09-01 against merged main commit
`c5ffa094d2b438416f9510abb69f552d3e0e3033`; release preparation was then merged as `8cd2797`.
The publication section below is a post-tag record committed after the immutable tag completed.

## Scope

The release adds opt-in tenant-safe Databricks billing attribution and a privileged, non-packable
audit-partition maintenance tool. It also contains the queued dependency and pinned-action updates
from pull requests #78, #79, #80 and #82. Every open feature and Dependabot pull request was
resolved before regression; `gh pr list --state open` returned an empty list.

## Repository gates

```text
dotnet restore LakeWright.slnx --locked-mode
dotnet restore scripts/load/Lakewright.LoadHarness/Lakewright.LoadHarness.csproj --locked-mode
dotnet build LakeWright.slnx -c Release --no-restore
dotnet build scripts/load/Lakewright.LoadHarness/Lakewright.LoadHarness.csproj -c Release --no-restore
dotnet format LakeWright.slnx --no-restore --verify-no-changes
dotnet list LakeWright.slnx package --vulnerable --include-transitive
dotnet list scripts/load/Lakewright.LoadHarness/Lakewright.LoadHarness.csproj package --vulnerable --include-transitive
dotnet list tools/LakeWright.DatabaseMaintenance/LakeWright.DatabaseMaintenance.csproj package --vulnerable --include-transitive
dotnet test LakeWright.slnx --no-build --no-restore -c Release --filter "Category!=Live" --logger trx --collect:"XPlat Code Coverage"
dotnet test LakeWright.slnx --no-build --no-restore -c Release --filter "Category=TenantIsolation" --logger trx
```

Result: locked restores passed. Both Release builds completed with zero warnings/errors. Formatting
passed. Every project reported no vulnerable direct or transitive package. The non-live suite
passed 251/251 and the independently gated tenant-isolation filter passed 209/209. TRX and
Cobertura artifacts were retained under the run's ignored `.git/codex-artifacts` directory.

The documentation scripts initially read ignored personal `local/` notes, making local results
depend on files outside the repository. They now enumerate tracked plus non-ignored new Markdown
files. With the release evidence present, 113 relative links/images resolved and no stale claims
were found. No
personal file was edited.

The audit partition subset passed 21/21 against PostgreSQL 17. Its negative cases include corrupt
lifecycle state, missing range index or registry primary key, disabled and replica-only identity
triggers, and loss of `SECURITY DEFINER`. The positive lifecycle includes populated copy/compare,
UTC month arithmetic across DST, bounded sparse history, ACL restoration, a non-superuser owner
with FORCE RLS-hidden rows, rollback, cleanup/remigration and finalize. CLI help exposed only
`migrate`, `validate`, `finalize`, `rollback` and `maintain` and executed no DDL.

## Load harness

```text
dotnet run --project scripts/load/Lakewright.LoadHarness/Lakewright.LoadHarness.csproj \
  -c Release --no-build -- --rps=50 --duration=30
```

Result: 1,202 operation creates and 297 cost reads completed with zero errors. Operations p99 was
16.3 ms, cost p99 was 10.5 ms, and peak PostgreSQL connections were 10/200 (5%). All four default
SLO gates passed. Testcontainers removed the run's PostgreSQL container.

## Packages and isolated consumer

```text
dotnet build LakeWright.slnx -c Release --no-restore -p:Version=1.1.0
dotnet pack LakeWright.slnx --no-build -c Release -o <packages> -p:Version=1.1.0
```

Seven `.nupkg` and seven `.snupkg` files were produced. Core and Embedding contained `net8.0` and
`net10.0`; AI, AspNetCore, Conversations, Databricks and Multitenancy contained `net10.0`.

An isolated `net8.0` console project used a NuGet configuration containing only the local artifact
directory and nuget.org, referenced `LakeWright.Embedding` 1.1.0, and checked both publish-gate
entry points. Restore passed and the program printed `Inspect=0, InspectAll=0,1`.

## Docker, HTTP and browser

Docker Desktop reported running with client/server 29.7.2. The Signalboard image rebuilt from the
pinned .NET 10 SDK and chiseled runtime digests under unique Compose project
`lakewright-v110-rc`. PostgreSQL became healthy and the application stayed running.

The HTTP regression returned 200 for health and OpenAPI; repeated creates returned 202 with the
same operation ID; a mismatched replay returned 422; Bob's cross-tenant read returned 404; and
Vera's Viewer write returned 403.

The existing Playwright smoke passed all 15 assertions: anonymous rendering, three identities,
interactive Alice operation creation without navigation, Viewer explanation and disabled control,
Bob's empty Globex view, clean consoles, dark theme, and no page-level overflow at 390px. All seven
screenshots were independently inspected and are retained with the
[dev-testing report](../../.codex/checklists/v1.1.0-whole-app-regression/report-2026-09-01_19-42.md).
The UI/UX classifier, rerun with evidence artifacts excluded, returned `SKIP`: no application
frontend file changed since v1.0.1.

Compose cleanup removed both run containers, its network and its volume; a final filtered query
reported zero of each. Unrelated containers were not stopped or modified.

## Databricks

The official Databricks CLI 1.14.1 binary authenticated through an existing OAuth profile whose
name is intentionally omitted. In `databricks/`:

```text
databricks bundle validate -t dev --var catalog=<temporary-validation-catalog> -p <workspace-profile>
databricks bundle plan -t dev --var catalog=<temporary-validation-catalog> -p <workspace-profile>
```

Validation returned `Validation OK`. The non-mutating plan returned two creates
(`schemas.reference`, `sql_warehouses.analytics`), zero changes and zero deletes. Nothing was
deployed.

Live billing is **SKIPPED**, not passed. A read-only catalog-grants lookup returned
`User does not have USE SCHEMA on Schema 'system.billing'`. All existing SQL warehouses were
stopped, so no warehouse was started merely to reproduce the same permission failure, and no
grant was added. The local suite remains the evidence for billing correctness until a disposable
non-production principal receives the documented system-table access.

## Hosted checks

PR #90 passed build, general tests, tenant isolation, docs, bundle, load, dependency review and
both CodeQL languages before merge. The resulting `main` CI run 33532296562 and Security run
33532296429 both completed successfully at `c5ffa094d2b438416f9510abb69f552d3e0e3033`.

## Publication

Pull request #104 passed CI and Security, had no reviewers assigned, and was squash-merged as
`8cd27977bd54f32546a741a2d5211f052ca1282e`. Main CI run 33534300771 and Security run
33534301030 then passed at that exact commit.

The annotated `v1.1.0` tag has a valid SSH signature for the repository signing identity and peels
to `8cd27977bd54f32546a741a2d5211f052ca1282e`. Release workflow run
[33534616649](https://github.com/ivanvyd/LakeWright.NET/actions/runs/33534616649) verified the tag,
restored in locked mode, built, passed the non-live suite, packed, generated and validated the
CycloneDX SBOM, created provenance, published the GitHub Release, exchanged OIDC for a short-lived
NuGet key, and pushed all seven primary plus seven symbol packages with HTTP 201.

The public [v1.1.0 release](https://github.com/ivanvyd/LakeWright.NET/releases/tag/v1.1.0) is neither
draft nor prerelease and contains seven primary packages plus `sbom.cyclonedx.json`. Every
downloaded SHA-256 digest matched GitHub's asset metadata. All seven packages passed
`gh attestation verify` constrained to `release.yml`, `refs/tags/v1.1.0`, and the exact source
digest above.

All seven versions appeared in the public NuGet V3 index. A fresh `net8.0` consumer used an empty
package cache and a NuGet configuration containing only nuget.org, restored
`LakeWright.Embedding` 1.1.0 with `--force --no-http-cache`, and printed
`Inspect=0, InspectAll=0,1`. `dotnet nuget verify --all` also accepted the downloaded package's
NuGet.org repository signature.
