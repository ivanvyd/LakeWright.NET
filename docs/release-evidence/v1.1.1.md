# 1.1.1 release evidence

Patch-release preparation on 2026-09-01. Publication evidence will be added after the immutable
signed-tag workflow completes.

## Scope

The v1.1.0 post-ship panel found that the billing report window was bounded only by the HTTP
endpoint. Direct consumers of the public attribution service or billing reader could therefore
request a multi-year account-wide scan. It also found no process-level protection against many
concurrent billing statements.

The patch moves the 31-day and one-day-future limits into a shared Core validator used by both the
tenant attribution service and Databricks reader. The HTTP layer retains the same early 400
responses. The billing reader is now a stateless singleton with a configurable process-wide bound
on concurrent and outstanding statement lifecycles. Saturation fails closed with transient code
`BILLING_BUSY`, mapped to HTTP 503. Billing submissions also use server-side timeout cancellation;
when a create response is lost, the local admission slot is held through that deadline before the
safe transient result `STATEMENT_CREATE_UNCERTAIN` is returned.

## Regression

Locked restores passed for the solution and load harness. Both Release builds completed with zero
warnings and errors; formatting passed; 114 documentation links/images resolved; claim checks
passed; and the solution, load harness and maintenance tool reported no vulnerable direct or
transitive packages.

Before merge, the full non-live suite passed 265/265 with TRX and Cobertura output retained under
`.git/codex-artifacts/release-v1.1.1/test-results-final5`. The independent tenant-isolation filter
passed 221/221, and the audit partition subset remained green at 21/21. The 48-test
billing/endpoint/configuration slice includes direct-service and direct-reader oversized/future
rejection, deterministic concurrency serialization, outstanding-work saturation, cancellation
after remote statement acceptance, ambiguous submission-failure handling, cancellation of a
blocked polling request at the overall deadline, safe 503 mapping and invalid-option startup
rejection.

Local packing produced seven `.nupkg` and seven `.snupkg` files at 1.1.1 under
`.git/codex-artifacts/release-v1.1.1/packages-final5`. A fresh `net10.0` consumer under
`.git/codex-artifacts/release-v1.1.1/consumer-final5` restored `LakeWright.AspNetCore` 1.1.1 from
those artifacts and printed
`Window=31, Submission=30, Concurrent=4, Outstanding=32`.

At 50 RPS for 30 seconds, the load harness completed 1,202 operation creates and 297 cost reads
with zero errors. Operation p99 was 11.2 ms, cost p99 was 8.8 ms, and peak PostgreSQL connections
were 9/200 (4.5%); every SLO passed and Testcontainers removed its PostgreSQL container.

## Post-merge regression

Pull request [#105](https://github.com/ivanvyd/LakeWright.NET/pull/105) passed every required check
and was squash-merged as `c6cafceac8f4dd6756d72536cc51ee34a671da9f`. Main
[CI run 33540862218](https://github.com/ivanvyd/LakeWright.NET/actions/runs/33540862218) and
[Security run 33540862188](https://github.com/ivanvyd/LakeWright.NET/actions/runs/33540862188)
both passed at that exact commit.

The locked solution and load-harness restores, Release builds, formatting, documentation checks
and vulnerability scans were repeated on the merged commit. The build again had zero warnings and
errors, and the full non-live and tenant-isolation filters again passed 265/265 and 221/221. TRX and
Cobertura output is retained under `.git/codex-artifacts/release-v1.1.1/postmerge-tests`.

Docker Desktop client and engine 29.7.2 built the pinned SDK/runtime image and started a clean
`lakewright-v111-final` Compose project. `/health` returned `Healthy` and OpenAPI returned HTTP 200.
The HTTP smoke proved same-key idempotency, different-payload 422, cross-tenant 404 and Viewer-write
403. Playwright passed all 15 assertions with no browser-console errors; the seven retained visual
states cover anonymous home/sign-in, admin light/dark/mobile, Viewer denial and the other tenant.
Visual inspection found no clipping outside the table's intentional narrow-screen scroll region.
Compose cleanup removed the run's two containers, network and volume.

The post-merge 50 RPS, 30-second load run completed 1,203 operation creates and 296 cost reads with
zero errors. Operation p99 was 27.0 ms, cost p99 was 26.6 ms, and peak PostgreSQL connections were
9/200 (4.5%); every SLO passed and Testcontainers removed its PostgreSQL container.

Local Databricks CLI 1.10.0 revalidated the bundle and produced the same non-mutating plan: two
creates (`schemas.reference`, `sql_warehouses.analytics`), zero changes and zero deletes. Nothing
was deployed. A read-only grants lookup reconfirmed that live billing remains **SKIPPED**, not
passed: the identity still lacks `USE SCHEMA` on `system.billing`. No warehouse was started and no
permissions were changed.

## Publication

Pending the signed `v1.1.1` tag, Release workflow, provenance verification, public NuGet
registration and isolated public-package consumer rerun.
